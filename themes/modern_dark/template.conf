# themes/modern_dark/template.conf

# Load the apps component
{{ apps }}

# TEST: Show the active window class dynamically
# This value comes from the 'active_window' provider in registry/context.toml
$active_window_class = {{ active_window }}

exec-once = echo "Theme Loaded Successfully!"
